{
  "routes": [
    {
      "method": "GET",
      "path": "/status",
      "handler": "Status.find",
      "config": {
        "policies": []
      }
    },
    {
      "method": "GET",
      "path": "/status/:_id",
      "handler": "Status.findOne",
      "config": {
        "policies": []
      }
    },
    {
      "method": "GET",
      "path": "/status/count",
      "handler": "Status.count",
      "config": {
        "policies": []
      }
    },
    {
      "method": "POST",
      "path": "/status",
      "handler": "Status.create",
      "config": {
        "policies": []
      }
    },
    {
      "method": "PUT",
      "path": "/status/:_id",
      "handler": "Status.update",
      "config": {
        "policies": []
      }
    },
    {
      "method": "DELETE",
      "path": "/status/:_id",
      "handler": "Status.destroy",
      "config": {
        "policies": []
      }
    }
  ]
}
